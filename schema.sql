CREATE TABLE movies (
    movie VARCHAR(255),
    comment VARCHAR(255),
   id VARCHAR(255),
   poster_path VARCHAR(255)
);

-- ALTER TABLE moviess
-- ADD id VARCHAR(255);

-- 	INSERT INTO moviess
-- VALUES (id);




-- CREATE UNIQUE INDEX id
-- ON movies (id);

